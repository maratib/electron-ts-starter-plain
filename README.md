# Electron starter plain with Typescript

### Build release (dmg) :

run watch sass

```bash
# For distribution
export CSC_IDENTITY_AUTO_DISCOVERY=false
export CSC_NAME="Your Name (Personal Team)"

npm run dist

```
